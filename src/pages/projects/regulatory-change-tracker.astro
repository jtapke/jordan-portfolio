---
import BaseLayout from '../../layouts/BaseLayout.astro';
import RegTrackerWrapper from '../../components/tracker/RegTrackerWrapper.astro';
---

<BaseLayout
  title="Regulatory Change Tracker | Jordan Tapke"
  description="A live dashboard pulling from FinCEN, OCC, CFPB, and FRB feeds to categorize regulatory updates by topic."
  currentPage="/projects"
>
  <section class="pt-20 pb-8 md:pt-24 md:pb-12 px-6">
    <div class="max-w-6xl mx-auto">
      <!-- Back Link -->
      <a
        href="/projects"
        class="inline-flex items-center gap-2 text-text-secondary hover:text-accent transition-colors text-sm mb-8"
      >
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Back to Projects
      </a>

      <!-- Project Header -->
      <div class="mb-8 md:mb-10">
        <p class="mono-label text-accent mb-3">Regulatory Intelligence</p>
        <h1 class="heading-xl text-text-primary mb-3">
          Regulatory Change Tracker
        </h1>
        <p class="mono-label text-text-secondary">
          Live updates from FinCEN, OCC, CFPB &amp; FRB
        </p>
      </div>
    </div>
  </section>

  <!-- Tracker Section -->
  <section class="px-4 md:px-6 pb-8">
    <div class="max-w-6xl mx-auto">
      <RegTrackerWrapper />
    </div>
  </section>

  <!-- Project Description -->
  <section class="px-6 pb-20 md:pb-28">
    <div class="max-w-3xl mx-auto">
      <div class="border-t border-border pt-10 md:pt-14">
        <h2 class="heading-lg text-text-primary mb-6">About This Project</h2>
        <div class="space-y-4 body-lg text-text-secondary">
          <p>
            Staying current on regulatory changes is essential for any compliance professional.
            This tool aggregates live RSS feeds from four key federal agencies &mdash; FinCEN,
            OCC, CFPB, and the Federal Reserve Board &mdash; and automatically categorizes
            each update by compliance topic.
          </p>
          <p>
            Filter by agency or topic area (BSA/AML, sanctions, consumer protection, and more)
            to quickly find the updates that matter most. All data is fetched in real-time
            directly from official government feeds.
          </p>
          <p>
            Built as a demonstration of how compliance teams can use lightweight tooling to
            stay ahead of regulatory changes without expensive subscription services.
          </p>
        </div>

        <!-- Tech Stack -->
        <div class="mt-10">
          <h3 class="heading-md text-text-primary mb-4">Built With</h3>
          <div class="flex flex-wrap gap-2">
            {['Preact', 'Astro', 'Tailwind CSS', 'RSS/XML', 'DOMParser'].map((tech) => (
              <span class="mono-label text-xs bg-accent-light/50 text-accent px-3 py-1.5 rounded-sm">
                {tech}
              </span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
